{
  "name": "predictcare-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve public",
    "build": "node scripts/generate-portfolio.js && mkdir -p public && test -f public/index.html && echo 'Static site ready in public/'",
    "start": "npx serve public"
  },
  "devDependencies": {
    "serve": "latest"
  }
}
